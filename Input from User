#include <stdio.h>
int main() {   
    int number;
   
    printf("Enter a number: ");  
    
    // reads and stores the number
    scanf("%d", &number);

    // displays output
    printf("You entered: %d", number);
    
    return 0;
}
